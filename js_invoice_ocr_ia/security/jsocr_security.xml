<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Category for JSOCR groups -->
        <record id="module_category_jsocr" model="ir.module.category">
            <field name="name">Invoice OCR IA</field>
            <field name="description">Automated invoice processing with OCR and AI</field>
            <field name="sequence">50</field>
        </record>

        <!-- Base user group: can view OCR jobs and validate their own invoices -->
        <record id="group_jsocr_user" model="res.groups">
            <field name="name">JSOCR / User</field>
            <field name="category_id" ref="module_category_jsocr"/>
            <field name="comment">Can view OCR import jobs and validate assigned invoices.</field>
        </record>

        <!-- Manager group: can manage all jobs and masks -->
        <record id="group_jsocr_manager" model="res.groups">
            <field name="name">JSOCR / Manager</field>
            <field name="category_id" ref="module_category_jsocr"/>
            <field name="implied_ids" eval="[(4, ref('group_jsocr_user'))]"/>
            <field name="comment">Can manage all OCR import jobs, masks, and corrections.</field>
        </record>

        <!-- Admin group: full access including configuration -->
        <record id="group_jsocr_admin" model="res.groups">
            <field name="name">JSOCR / Admin</field>
            <field name="category_id" ref="module_category_jsocr"/>
            <field name="implied_ids" eval="[(4, ref('group_jsocr_manager'))]"/>
            <field name="comment">Full access to JSOCR configuration and all features.</field>
        </record>

    </data>
</odoo>
